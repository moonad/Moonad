VoxModel.set_col(idx: U32, col: U32, vox: VoxModel): VoxModel
  case vox:
  | let vox.buffer = Buffer32.set(U32.add(U32.mul(idx,U.32(0d2)),U.32(0d1)), col, vox.buffer)
    VoxModel.new(vox.size, vox.buffer);
