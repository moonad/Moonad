type Monad<M: Type -> Type> {
  monad(
    bind: <A: Type, B: Type> -> M(A) -> (A -> M(B)) -> M(B),
    pure: <A: Type> -> A -> M(A)
  )
}

Monad.pure<M: Type -> Type>(m: Monad(M)): <A: Type> -> A -> M(A)
  case m {
    monad: m.pure
  }

